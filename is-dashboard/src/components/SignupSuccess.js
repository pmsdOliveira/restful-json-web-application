import React from "react";
import { Link } from "react-router-dom";

export default function SignupSuccess() {
    return (
        <Link to="/" className="signup-success">Task failed successfully.
        </Link>
    );
}
